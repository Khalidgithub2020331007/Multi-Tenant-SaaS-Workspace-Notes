
const RightSide = ({ currentWorkspace }: { currentWorkspace: string }) => {

  return (
    <div className="space-y-4">
      <div className="text-lg font-bold">Current Workspace: {currentWorkspace}</div>
      <div className="text-lg font-bold">Available Workspaces:</div>
     Hello bd
    
    </div>
  );
};

export default RightSide;